Code organization
=================

Branches:

 * `master` -> Code candidate for a stable version
 * `experimental` -> For the faint hearted, where the hardcore development occurs.
 * `php52` -> Code for backwards compatiblity with PHP 5.2. Discontinued branch.